pluginManagement {
    plugins {
        id("com.netflix.nebula.oss.settings") version("25.+")
        id("com.netflix.nebula.plugin-plugin") version ("25.+")
    }
}
plugins {
    id("com.netflix.nebula.oss.settings")
}

rootProject.name='gradle-dependency-lock-plugin'
